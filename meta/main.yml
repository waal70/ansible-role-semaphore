galaxy_info:
  author: waal70 (Andre)
  description: Role to install Ansible Semaphore UI using Portainer on Debian systems.
  company: waal70
  platforms:
    - name: Debian
      versions:
        - trixie

  license: GPL

  min_ansible_version: "2.1"

  galaxy_tags: [debian,
                portainer,
                semaphore,
                ansible
  ]

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
